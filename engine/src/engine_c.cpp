#include "engine_c.h"
#include "engine.h"

// The implementation is already in engine.cpp
// This file exists to ensure the C ABI symbols are exported

// Re-export all functions (they're already implemented with C linkage in engine.cpp)
